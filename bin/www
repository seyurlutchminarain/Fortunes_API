#!/usr/bin/env node
// The above line tells the computer to run the app using node
const app = require("../app");

const port = 3000;

app.listen(port, () => console.log(`Listening on port: ${port}`)); // specify the port number. Default hostname is localhost
